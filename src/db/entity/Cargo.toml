[package]
name = "entity"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
sea-orm = { version = "^0.12.11", features = ["sqlx-postgres", "runtime-tokio-native-tls", "macros", "with-chrono"] }
